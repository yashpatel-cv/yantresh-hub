# Real secrets/config — copy .env.example to .env and fill it in locally.
.env

# Local state-volume backups written by deploy/backup-state.sh.
backups/

# Submodule checkouts are tracked by gitlink, not content.
__pycache__/
*.pyc
